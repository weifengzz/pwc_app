import { ViewStyle } from 'react-native'

/**
 * 广告界面样式
 */

export interface IAdsStyle {
  container: ViewStyle
}

export const AdsStyle = {
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center'
  }
}

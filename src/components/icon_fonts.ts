/**
 * 自定义字体文件
 */
import { createIconSet } from 'react-native-vector-icons'
import glyphMap from '../glyphmaps/icon_fonts'
const iconSet = createIconSet(glyphMap, 'Iconfont', 'Iconfont.ttf')
export default iconSet
